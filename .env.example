# Generate with: openssl rand -base64 32
# Losing this key loses every key wrapped by keeper, and with them every object in sealbox.
KEEPER_MASTER_KEY=
# Development only: one token allowed every key. Production uses KEEPER_CLIENTS_FILE.
KEEPER_TOKEN=
